Python 2.7 official documentation notes
======
* python is:
	* dynamically typed -> doesn't use explicit datatype declarations
	* stringly typed -> once a variable has a datatype it actually matters
	
